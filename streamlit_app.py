import streamlit as st

# Interface
severe_wasting = st.number_input('Severe wasting (%)')
wasting = st.number_input('Wasting (%)')
stunting = st.number_input('Stunting (%)')
underweight = st.number_input('Underweight (%)')
overweight = st.number_input('Overweight (%)')